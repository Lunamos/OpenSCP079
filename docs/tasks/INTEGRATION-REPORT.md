# Integration report — ctx-design + server-gateway

Date: 2026-06-11
Branch: `main`

## Merge order

1. Preserved a pre-existing dirty main-worktree state in `stash@{0}` (`pre-integration dirty main state before T3`) before starting the required clean merge flow. The dirty state was a deleted root `CLAUDE.md` plus untracked `docs/CLAUDE.md` and `docs/context-design.md`; no branch/worktree was deleted.
2. Read `CLAUDE.md` after restoring the clean main worktree.
3. Baseline exact command `uv sync && uv run python -m pytest -q` did not reach tests because plain `uv sync` removes the `dev` extra and therefore `pytest` is absent. With `uv sync --extra dev`, the baseline main suite showed the known locale-sensitive default-card failures under this shell's `LC_ALL=C.UTF-8` (`102 passed, 3 failed`).
4. Merged `ctx-design` first. This was a fast-forward from `89acb89` to `a2478c2`.
5. Merged `server-gateway` second. The server merge had no manual conflict markers; the final amended integration merge commit is the commit containing this report.

## Conflicts / integration fixes

- `ctx-design`: no conflicts.
- `server-gateway`: no textual conflicts. `README.md` and `README.zh-CN.md` auto-merged and kept both roadmap/status items: the three-zone/card-first context item and the remote JSON-RPC gateway foundation item.
- `uv.lock` was regenerated by `uv sync --extra dev --extra server` so the new optional `server` extra's `websockets>=12` dependency is locked. This is the only post-merge file fix.
- Cross-checked the server seam: `src/lunamoth/server/dispatch.py` talks to the runtime only through `lunamoth.protocol.api.CharaHandle`; the ctx-design changes did not require a CharaHandle surface change, and the real mock-handle server test passes.

## Verification

After `ctx-design`:

- `uv run python -m pytest -q` — 114 passed.
- `uvx ruff check --select F src/lunamoth tests` — clean.

After both merges and the lockfile fix:

- `uv sync --extra dev --extra server` — installed the optional WebSocket dependency and updated `uv.lock`.
- `uv run python -m pytest -q tests/test_architecture.py` — 5 passed.
- `uv run python -m pytest -q` — 122 passed.
- `uvx ruff check --select F src/lunamoth tests` — clean.
- `uv run lunamoth version` — `lunamoth 0.1.0`.
- `uv run lunamoth doctor` — exit 0; reports Python, uv, git checkout, and `sandbox-exec` OK; Docker is missing but marked optional.

## Final test count

- Full suite: 122 passed.
- Architecture subset: 5 passed.

## Still open / notes

- No unresolved design clash was found.
- The repo's documented baseline command still needs a dev-extra install for tests in this environment: `uv sync` alone removes `pytest`, while `uv sync --extra dev` installs it.
- This report leaves the preserved pre-integration stash in place for operator review; it was not re-applied because it would reintroduce non-branch dirty files.
- No push performed. Worktrees and branches were left intact.
